# Filename specification

Observed daily and weekly outputs are `mintemp_YYYY-MM-DD.tif` and `mintemp_YYYY-Www.tif`. Future analogs use `_est` immediately before `.tif`. ISO week-years and two-digit week numbers are required; `.tiff` is accepted only during inventory.
