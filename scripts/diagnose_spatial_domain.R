root <- normalizePath(".",winslash="/"); invisible(lapply(list.files("R",pattern="\\.R$",full.names=TRUE),source)); cfg<-resolve_config_paths(read_pipeline_config("config/era5_mintemp.yml"),root); cfg<-validate_pipeline_config(cfg); d<-diagnose_spatial_domain(cfg$spatial$template_path,cfg$spatial$bbox_path); print(d[c("template_crs","template_is_geographic","extent","resolution","dimensions","origin","ncell","non_na_count","cds_area")]); fs::dir_create(cfg$paths$run_dir,recurse=TRUE); jsonlite::write_json(d,file.path(cfg$paths$run_dir,"spatial_diagnostics.json"),pretty=TRUE,auto_unbox=TRUE)
